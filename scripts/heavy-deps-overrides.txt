# uv pip compile --override file — pinned overrides used during resolution
# of the heavy stack. Currently empty; reserved for future numpy/numba
# version conflicts when adding GFPGAN / LatentSync deps.
